<%= content_for(:navbar_class, "navbar-home") %>

<div class="home-banner">
  <div class="container">
    <h1>Shaka</h1>
    <p>Let’s find the best spots for wind sports based on you</p>
    <div class="col-xs-12 col-sm-6 col-sm-offset-6 p-0">
      <div class="search-form">
        <%= form_tag spots_path, method: :get, class: "form-inline" do %>
          <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Find a spot near you" %>
            <%= submit_tag "Search", class: "btn-home-red" %>
        <% end %>
      </div>
    </div>
    <p> or </p>
    <% if user_signed_in? %>
      <%= link_to "Find me a spot", spots_path(params: {skill_discipline: true}), class: "btn btn-danger" %>
    <% else %>
      <%= link_to "Create your profile", new_user_registration_path, class: "btn btn-danger" %>
    <% end %>
  </div>
</div>
